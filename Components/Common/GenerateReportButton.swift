import SwiftUI

struct GenerateReportButton: View {
    var body: some View {
        Button("生成综合健康报告") {
            // TODO: 跳转 Report 页面
        }
        .frame(maxWidth: .infinity)
        .padding()
        .background(Color.blue)
        .foregroundColor(.white)
        .cornerRadius(14)
    }
}
